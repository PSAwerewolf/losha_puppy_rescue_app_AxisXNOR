name: losha_puppy_rescue_app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  image_picker: ^0.8.6
  email_auth: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.1"

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/Logo/puppy.png"
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/Logo/puppy.png
    - assets/Logo/puppy.jpg
